# Copy to .env.local (gitignored) and paste real keys.

# Google AI Studio key — LLM for vision parse + text calls (aistudio.google.com)
VITE_GEMINI_API_KEY=

# Gradium key — speech-to-text (gradium.ai)
VITE_GRADIUM_API_KEY=
